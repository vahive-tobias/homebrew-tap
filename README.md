# vahive-tobias Homebrew Tap

This repository contains the official Homebrew package formula for **Magus OpenSecMCP**.

## Installation

To install the deterministic execution firewall globally on macOS, run the following commands in your terminal:

```bash
brew tap vahive-tobias/tap
brew install magus-opensecmcp
